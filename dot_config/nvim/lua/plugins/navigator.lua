-- vim-tmux-navigator: Seamless navigation between tmux panes and vim windows
return {
  {
    "christoomey/vim-tmux-navigator",
    lazy = false,
    config = function()
      -- The plugin works by default with C-h, C-j, C-k, C-l
      -- No additional configuration needed
    end,
  },
}
