# ===================================================================
# ===                       CORE SETTINGS                         ===
# ===================================================================

# Set zsh as the default shell
set -g default-shell /bin/zsh

# Use 256-color terminal and set terminal overrides for italics
set -g default-terminal "tmux-256color"
set -as terminal-overrides ",xterm*:RGB"

# Set prefix to C-a
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Increase history limit
set -g history-limit 20000

# Start windows and panes at 1 for easier referencing
set -g base-index 1
setw -g pane-base-index 1

# Enable mouse support
set -g mouse on

# Reduce escape time for faster responsiveness (especially in vim)
set -s escape-time 0

# Don't automatically rename windows
setw -g allow-rename off
setw -g automatic-rename off

# Set terminal window title
set -g set-titles on
set -g set-titles-string "#I:#W"

# ===================================================================
# ===                        KEYBINDINGS                          ===
# ===================================================================

# --- Config & Session Management ---
bind r source-file ~/.config/tmux/tmux.conf \; display "Reloaded!"
bind Q confirm-before -p "kill-session #S? (y/n)" kill-session

# NEW: Visual tree for choosing sessions/windows
bind s choose-tree -s

# --- Window Management ---
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind X kill-window
bind C-x confirm-before -p "kill other windows? (y/n)" "kill-window -a"

# Navigate windows
bind h previous-window
bind l next-window

# Move (swap) windows with or without prefix
bind H swap-window -t -1\; select-window -t -1
bind L swap-window -t +1\; select-window -t +1
bind -n M-H swap-window -t -1\; select-window -t -1
bind -n M-L swap-window -t +1\; select-window -t +1

# Rename windows and sessions
bind n command-prompt -I "#{window_name}" "rename-window '%%'"
bind N command-prompt -I "#{session_name}" "rename-session '%%'"

# --- Pane Management ---
bind x kill-pane
bind + resize-pane -Z # Zoom pane

# Navigate panes with vim-style keys
bind C-h select-pane -L
bind C-j select-pane -D
bind C-k select-pane -U
bind C-l select-pane -R

# Resize panes
bind C-i resize-pane -U 5
bind C-u resize-pane -D 5
bind C-y resize-pane -L 5
bind C-o resize-pane -R 5

# NEW: Toggle synchronized typing to all panes
bind-key y set-window-option synchronize-panes

# ===================================================================
# ===                        COPY MODE                            ===
# ===================================================================
setw -g mode-keys vi
set-option -s set-clipboard on # Use system clipboard

bind v copy-mode
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel
bind P paste-buffer

# ===================================================================
# ===                    THEMING & STATUS BAR                     ===
# ===================================================================
set-option -g status-position top
set -g status-interval 2

# --- Conditional Colors (Local vs SSH) ---
%if #{==:#{SSH_CLIENT},} # Remote Colors
    color1=colour237; color2=colour237; color3=colour7; color4=colour167; color5=colour223; color6=colour214; color7=colour0; color8=colour12; color9=colour240; color10=colour234; color11=colour255; color12=colour243; color13=colour254; color14=colour248; color15=colour239; color16=colour109; color17=colour235; color18=colour124; color19=colour222; color20=colour0
%else # Local Colors
    color1=colour23; color2=colour43; color3=colour16; color4=colour167; color5=colour0; color6=colour23; color7=colour23; color8=colour12; color9=colour240; color10=colour39; color11=colour255; color12=colour114; color13=colour23; color14=colour248; color15=colour15; color16=colour109; color17=colour235; color18=colour124; color19=colour222; color20=colour0
%endif

# --- UI Element Colors ---
set-option -g status-style "bg=$color1,fg=$color5"
set-option -g pane-active-border-style "fg=$color6"
set-option -g pane-border-style "fg=$color15"
set-option -g message-style "bg=$color15,fg=$color5"
set-window-option -g window-status-current-style "bg=$color18,fg=$color1"

# --- Status Bar Content ---
set -g status-left-length 150
set -g status-right-length 30
set-option -g status-left "#[fg=$color3, bg=$color2]#{?client_prefix,#[bg=$color4],}  #H #S #[fg=$color2, bg=$color1]#{?client_prefix,#[fg=$color4],}#{?window_zoomed_flag, 🔍,}"
set-option -g status-right "#[fg=$color6, bg=$color1] #[fg=$color7, bg=$color6]   %b %d %Y #[fg=$color8,bg=$color6]#[fg=$color20,bg=$color8]   %H:%M "
set-window-option -g window-status-current-format "#[fg=$color1, bg=$color10]#[fg=$color11, bg=$color10, bold] #W #[fg=$color10, bg=$color1]"
set-window-option -g window-status-format "#[fg=$color1,bg=$color12,noitalics]#[fg=$color13, bg=$color12] #W #[fg=$color12, bg=$color1]"

# ===================================================================
# ===                         PLUGINS                             ===
# ===================================================================

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'

# --- Plugin Settings ---
# For tmux-resurrect
set -g @resurrect-save 'S'
set -g @resurrect-restore 'R'

# SUGGESTION: Add this for automatic saving
# set -g @plugin 'tmux-plugins/tmux-continuum'
# set -g @continuum-restore 'on'

# SUGGESTION: For seamless navigation between vim and tmux panes
# set -g @plugin 'christoomey/vim-tmux-navigator'

# Initialize TPM (must be the last line)
run '~/.config/tmux/plugins/tpm/tpm'